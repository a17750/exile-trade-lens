{
  "name": "exile-trade-lens",
  "private": true,
  "type": "module",
  "scripts": {
    "build:extension": "tsc -p tsconfig.json",
    "check:extension": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.7.2"
  }
}
